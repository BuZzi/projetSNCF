<!--
A enlever après :
latitude : {{ latitude }}
longitude : {{ longitude }}
-->
<div class="large-12 small-12 columns">
    <h4>
        <i class="foundicon-compass"></i>
        Gares proches de votre position
    </h4>
    <ul class="nav nav-list">
        {% for station in liste_stations %}
            <li>
                <a href="{{ path('lpdw_categories', {'name': station.name, 'latitude': station.latitude, 'longitude': station.longitude}) }}">
                    {{ station.name }}
                </a>
            </li>
        {% endfor %}
    </ul>
</div>
